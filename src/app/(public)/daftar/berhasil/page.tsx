import { PendaftaranSuccess } from "@/components/forms/pendaftaran-success";

export const metadata = {
  title: "Pendaftaran Berhasil",
  robots: { index: false },
};

export default function PendaftaranBerhasilPage() {
  return <PendaftaranSuccess />;
}
